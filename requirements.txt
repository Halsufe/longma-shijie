# 龙马·视界 后端核心依赖
# 安装方式：pip install -r requirements.txt
# 向量检索相关（sentence-transformers、torch）体积大、耗时久，已拆到 requirements-optional.txt，按需安装。

# Web 框架
fastapi==0.115.0
uvicorn[standard]==0.30.6

# 数据库 + ORM + 迁移
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9

# 数据校验
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# HTTP 客户端（AI 真实调用 + MCP 接口）
httpx==0.28.1

# 文件上传 / 解析
python-multipart==0.0.9
pypdf==5.0.0
docx2txt==0.8
openpyxl==3.1.5
xlrd==2.0.1
xlwt==1.3.0

# 环境变量
python-dotenv==1.0.1
