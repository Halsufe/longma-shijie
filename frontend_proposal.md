# 龙马·视界前端主题与布局优化需求文档

> 文档状态：已确认，可进入实现
> 更新日期：2026-08-07
> 适用项目：`BD/LM_SJ`
> 目标：在不改变业务能力的前提下，统一全站视觉主题、优化信息布局，并适配桌面、平板和手机。

## 1. 已确认决策

| 项目 | 决策 |
| --- | --- |
| 改版范围 | 覆盖登录页及全部业务界面 |
| 改动边界 | 视觉与布局为主；允许为适配布局调整前端交互，但不得改变接口、数据、权限和业务结果 |
| 用户角色 | 学生、教师、校友、管理员均需获得一致的基础体验 |
| 产品名称 | `龙马·视界` |
| 机构信息 | `中央财经大学管理科学与工程学院` |
| 班级信息 | `大数据管理与应用` |
| 色彩方向 | 以院徽/班徽的深蓝、灰蓝、亮蓝为主，允许少量渐变和金色点缀 |
| 徽标 | 使用院徽；替换现有产品图标；班徽不作为主要界面标识 |
| 布局策略 | 保留“左侧导航 + 顶部栏 + 内容区”的信息架构，进行保守优化 |
| 移动端 | 使用抽屉式侧栏 |
| 风格 | 年轻校园感、现代化、清晰、轻量、有适度科技感 |
| 浏览器 | 支持常见版本的 Edge、Chrome、360 浏览器 |
| 功能约束 | 保持现有功能可用，避免视觉改版引入回归问题；允许新增字体、图标库或其他依赖 |

## 2. 当前实现基线

当前前端为原生 HTML/CSS/ES Module，无前端框架。静态目录由后端挂载为 `/static`。

当前路由及页面：

- `overview`：工作台
- `chat`：AI 对话
- `knowledge`：知识库
- `courses`：课程与作业
- `community`：成果与社区
- `mentorship`：教师与计划
- `party`：党建（按现有角色权限显示）
- `notifications`：通知中心
- `profile`：个人设置
- `admin`：管理后台（管理员）

现有全局样式位于 `frontend/assets/styles.css`，应用壳层和登录页由 `frontend/js/app.js` 渲染。现有移动端已具备侧栏抽屉基础逻辑，本次主要统一样式、层级和断点行为。

徽标资源：

- 源文件：`image/院徽.jpg`
- 班徽源文件：`image/班徽.jpg`（本次不作为主品牌标识）
- 推荐静态访问路径：`/static/assets/院徽.jpg`。实现时将院徽放入 `frontend/assets/`，或通过等效静态路径提供，避免依赖项目外部绝对路径。

## 3. 目标与非目标

### 3.1 目标

1. 建立统一的院系品牌识别：所有页面都能稳定看到“龙马·视界”和院徽，必要时显示学院名称。
2. 让信息更适合扫描：标题、统计、待办、通知、快捷入口和业务列表形成明确层级。
3. 降低页面跳转和操作成本：导航、当前页面状态、主要操作、返回和关闭行为保持一致。
4. 在桌面、平板和手机上保持可读、可点击、不横向溢出。
5. 以可复用 CSS token 和组件类实现改版，便于后续页面继续扩展。

### 3.2 非目标

- 不修改后端 API、数据库、权限判定、业务计算和数据字段。
- 不改变路由名称、表单提交语义、文件上传下载行为、聊天会话数据结构。
- 不重写为新的前端框架；除非实现阶段证明必要，否则沿用现有原生 HTML/CSS/JS。

## 4. 品牌与视觉方向

### 4.1 品牌文案

- 主标题：`龙马·视界`
- 学院副标题：`中央财经大学管理科学与工程学院`
- 班级/空间标识：`大数据管理与应用`
- 页面标题使用简短业务名称，例如“工作台”“AI 对话”“知识库”，避免把机构全称重复塞入每个标题。

### 4.2 建议颜色 token

颜色是基于现有院徽和班徽的深蓝、灰蓝、亮蓝提炼的首版方案，实际开发可在视觉验收时微调，但必须保持语义一致。

```css
:root {
  --brand-900: #14233f;       /* 侧栏、深色品牌区 */
  --brand-800: #1d3157;
  --brand-700: #244779;       /* 主按钮、当前导航 */
  --brand-500: #2f8fc4;       /* 链接、图标、焦点 */
  --brand-400: #43b6dc;       /* 高亮、进度、科技感点缀 */
  --brand-100: #eaf5fb;
  --brand-050: #f4f9fc;
  --accent-gold: #d3a64a;     /* 仅作少量强调，不作为大面积背景 */
  --bg: #f5f7fa;
  --surface: #ffffff;
  --surface-soft: #f0f4f8;
  --surface-strong: #e4ebf2;
  --text: #172235;
  --text-soft: #5f6d7e;
  --text-faint: #8b98a8;
  --line: #dce4ec;
  --line-strong: #c5d1dd;
  --success: #19866a;
  --warning: #b77a19;
  --danger: #c45549;
  --info: #2f8fc4;
}
```

要求：

- 深色背景上使用白色或浅灰文字；正文文字与背景保持足够对比度。
- 渐变只用于品牌区、主按钮 hover、统计图形或装饰性背景，不用于承载长文本。
- 状态颜色继续语义化：成功为青绿、警告为琥珀、危险为珊瑚红、信息为亮蓝。
- 不使用过多发光、玻璃拟态或大面积装饰，确保校园产品的可信度和可读性。

### 4.3 字体、圆角与层次

- 字体优先使用系统中文字体：`Inter`、`Microsoft YaHei`、`PingFang SC`、`Noto Sans CJK SC`，不得依赖网络字体才能正常显示。
- 正文 14px，辅助文字 12px，页面主标题 24-28px，移动端主标题不低于 20px。
- 普通卡片和面板圆角 8px；输入框和按钮 6-8px；头像可使用圆形。
- 阴影轻量化：面板使用细边框为主，悬浮层、抽屉和弹窗再使用明显阴影。
- 所有文字、按钮和标签不得因视口变化溢出父容器；长标题使用换行或省略号。

## 5. 全局壳层需求

### 5.1 登录页

- 保留左右分栏的信息结构，但缩减装饰性文案，突出院徽、产品名和登录表单。
- 左侧品牌区显示院徽、`龙马·视界`、学院名称和一句简短欢迎语；不再展示与产品无关的虚构统计数字。
- 右侧表单保留学号/工号、密码、显示密码、登录、首次登录改密等现有流程，仅优化标签、间距、错误提示和加载态。
- 院徽在浅色或深色背景上均需保持清晰，禁止拉伸变形；必要时使用 `object-fit: contain` 和白色保护底。
- 手机端改为单列：品牌信息置于表单上方，确保首屏能看到登录按钮，避免横向滚动。

### 5.2 桌面应用壳层

- 保留固定左侧导航和顶部栏，导航宽度建议 232-248px；内容区使用最大宽度约 1480px 并居中。
- 侧栏顶部显示院徽（建议 40-44px）、`龙马·视界` 和学院短名；底部保留当前用户、角色和退出入口。
- 导航按“主要功能 / 管理”分组，当前项使用浅色底和品牌色文字，hover/focus 状态明显但不跳动。
- 顶部栏显示当前页面标题、日期、通知入口；不重复显示过长机构文案。
- 内容区统一使用页面标题区、操作区、内容面板三段结构；页面间的水平边距、垂直节奏一致。

### 5.3 移动端与平板

- 小于 860px 时侧栏默认收起，顶部栏显示菜单按钮；打开后侧栏从左侧滑入，覆盖层可点击关闭。
- 小于 620px 时隐藏非必要日期文字，页面操作按钮允许换行或变为图标按钮；统计卡片使用两列或单列。
- 平板优先保持两列内容布局，空间不足时再堆叠；聊天会话列表使用抽屉，不与消息区同时挤压。
- 抽屉打开时禁止页面背景滚动，关闭后恢复；支持 Esc 关闭并将焦点返回菜单按钮。

## 6. 页面级布局需求

### 6.1 工作台（overview）

首屏优先级固定为：成果统计、快捷入口、党建动态、AI 对话入口、待办、通知。

- 顶部保留欢迎语和当前身份信息，右侧放置最常用的主操作。
- 第一行使用 4 个统计卡（窄屏降为 2/1 列），突出成果、课程/作业、未读通知等真实数据；禁止虚构数据。
- 第二行采用“主要内容 + 辅助栏”：主要内容放成果趋势或近期成果，辅助栏放快捷入口和 AI 对话入口。
- 党建动态、待办、通知使用列表或时间线呈现，显示状态、时间和明确的进入/查看动作。
- 卡片之间保持统一标题栏、留白和操作位置，避免卡片嵌套卡片。

### 6.2 AI 对话（chat）

- 保留会话列表 + 消息区结构；桌面端左右分栏，移动端会话列表改为抽屉。
- 消息区区分用户消息、AI 消息、引用/附件、生成中和错误状态；重新生成、复制、停止生成等现有操作沿用原语义。
- 输入区固定在底部，发送按钮在禁用、发送中、失败时均有明确状态；长文本和代码块不得撑破窗口。
- 对话页保持足够的垂直空间，避免顶部标题和输入框占用过多高度。

### 6.3 知识库（knowledge）

- 顶部为作用域切换、搜索、上传/新建操作；文件夹路径和当前筛选状态可见。
- 文件列表统一使用表格或紧凑列表，文件类型、大小、更新时间、操作列对齐。
- 预览、下载、移动、删除等现有操作使用统一图标和 tooltip；预览弹窗在手机端改为全屏。
- 空状态、加载状态、无权限状态和上传失败状态使用统一状态面板。

### 6.4 课程与作业（courses）

- 课程筛选与作业列表位于同一工具栏，移动端按上下顺序排列。
- 作业项优先展示课程名、截止时间、提交状态和附件入口；逾期、待提交、已提交等状态颜色统一。
- 作业详情、附件预览和提交弹窗遵循统一面板/弹窗规范，不改变上传和提交逻辑。

### 6.5 成果与社区（community）

- 默认突出成果统计、分类筛选和时间线/列表；支持按现有字段筛选和排序。
- 成果卡片优先显示标题、类别、级别、日期、证明材料入口；标签数量过多时折叠。
- 上传、编辑、审核等按钮遵循角色权限，不因视觉重排而改变可见性。

### 6.6 教师与计划（mentorship）

- 教师/导师信息采用清晰的头像、姓名、研究方向和可操作入口；避免大面积装饰图片。
- 计划与推荐内容使用时间线或分组列表，突出下一步行动和截止时间。
- 筛选、匹配、查看详情等动作在桌面和移动端保持同一语义。

### 6.7 党建（party）

- 使用品牌深蓝搭配红色语义点缀，避免整页大红；统计、活动、材料和个人状态分区呈现。
- 党建动态优先显示在工作台辅助区，进入党建页后再展示完整列表和详情。
- 角色权限、报名、签到、材料和审核流程只改视觉承载，不修改现有数据和接口。

### 6.8 通知中心（notifications）

- 使用未读强调、已读弱化、按时间分组的列表结构；保留顶部未读数量。
- 查看详情、全部已读、关联页面跳转等操作位置固定；移动端支持整行点击。

### 6.9 个人设置（profile）

- 个人资料、账号安全和偏好设置分组展示；编辑使用统一弹窗或右侧面板。
- 头像、院徽品牌和个人身份信息在视觉上区分，不能将院徽误当作用户头像。
- 保留首次登录修改密码、资料保存和错误提示行为。

### 6.10 管理后台（admin）

- 管理页面沿用现有 Tab、筛选、表格和统计能力，统一表头、筛选器、分页和危险操作确认样式。
- 管理区使用更高信息密度，但与普通页面共享颜色 token、按钮、弹窗和状态规范。
- 危险操作使用珊瑚红并要求二次确认；不得仅依赖颜色传达风险。

## 7. 组件与交互规范

- 按钮：主操作使用实心品牌色，次操作使用描边，低频操作使用 ghost；图标按钮必须提供 `aria-label` 和 tooltip。
- 输入框：统一高度、边框、焦点环、错误态和帮助文字；错误信息紧邻字段显示。
- 面板：使用 `.panel` 类或等效组件，禁止在页面区块外再套无意义的卡片容器。
- 标签/徽章：只用于状态、类别和数量；同一语义在全站使用同一颜色。
- 表格：窄屏允许容器内横向滚动，不允许页面整体横向溢出；操作列保持可见。
- 弹窗/抽屉：支持关闭按钮、遮罩点击、Esc 和合理的焦点顺序；移动端弹窗可全屏化。
- 状态：至少覆盖 loading、empty、error、permission denied、success toast 五种状态。
- 动效：使用 150-250ms 的淡入、滑入、颜色过渡；不得影响表单提交、聊天滚动和键盘操作。

## 8. 响应式验收尺寸

| 区间 | 规则 |
| --- | --- |
| >= 1200px | 固定侧栏、顶部栏；工作台可使用 4 列统计和 2 列内容 |
| 861-1199px | 固定侧栏；统计降为 2 列；内容列按最小宽度自动堆叠 |
| 601-860px | 侧栏抽屉；顶部显示菜单；聊天会话列表抽屉化；内容区单列优先 |
| <= 600px | 单列表单和面板；统计 1-2 列；按钮可换行；日期等次要信息隐藏 |

所有断点均需验证 320px、375px、768px、1024px、1440px 五个代表尺寸；不能出现内容遮挡、文字溢出、点击区域过小或滚动锁死。

## 9. 资源与实现约束

1. 将 `image/院徽.jpg` 作为全站品牌图，建议复制到 `frontend/assets/院徽.jpg` 后以 `/static/assets/院徽.jpg` 引用；保留原始文件，不修改原图。
2. 更新 `frontend/index.html` 的 favicon 和主题色配置，使其与新品牌色一致。
3. 更新 `frontend/assets/styles.css` 的 CSS token 和全局组件样式；优先复用现有类名，减少业务视图改动。
4. 更新 `frontend/js/app.js` 的品牌文字、徽标路径和移动端菜单文案；不改变路由过滤、权限判定和 API 调用。
5. 各 `frontend/js/views/*.js` 仅在布局容器、标题顺序、操作区和状态展示需要时修改；不得删除现有业务入口。
6. 若引入图标或字体依赖，必须提供本地或可离线回退方案，并说明新增依赖。
7. 所有新增中文文件使用 UTF-8 编码，修复本次触及区域中可见的乱码文案，但不得借机改动无关业务文本。

## 10. 实施顺序

1. 建立品牌资源路径和全局 CSS token，完成 favicon、院徽、主题色替换。
2. 改造登录页和应用壳层，先验证导航、顶部栏、移动端抽屉及退出流程。
3. 统一基础组件：按钮、输入、面板、表格、标签、状态面板、弹窗和 toast。
4. 按工作台、AI 对话、知识库、课程、成果、教师计划、党建、通知、个人设置、管理后台顺序逐页调整布局。
5. 进行跨角色和跨视口回归，最后清理重复样式、无效 token 和资源引用。

## 11. 验收标准

### 11.1 视觉一致性

- [ ] 所有页面和登录页使用同一套品牌色、字体、圆角、边框、阴影和状态语义。
- [ ] 登录页、侧栏、favicon 和顶部品牌标识均使用院徽，不再使用绿色 `brand-mark.svg` 作为主标识。
- [ ] 页面显示准确文案：`龙马·视界`、`中央财经大学管理科学与工程学院`、`大数据管理与应用`。
- [ ] 工作台首屏按确认优先级呈现成果统计、快捷入口、党建动态、AI 对话入口、待办和通知。

### 11.2 交互与响应式

- [ ] 桌面端保留左侧导航 + 顶部栏 + 内容区结构，当前路由和角色权限行为不变。
- [ ] 手机端可以打开/关闭抽屉式侧栏，遮罩、Esc 和返回焦点行为正常。
- [ ] 320px 至 1440px 代表尺寸下无页面级横向滚动，文字和按钮不溢出。
- [ ] 聊天、上传、预览、表单、弹窗、通知和管理操作的原有成功/失败流程均可完成。
- [ ] 加载、空数据、错误、无权限、禁用和提交中状态均有清晰视觉反馈。

### 11.3 质量与回归

- [ ] Edge、Chrome、360 浏览器完成登录、导航、工作台、AI 对话、文件预览、课程提交和管理员页面冒烟测试。
- [ ] 浏览器控制台无因本次改版新增的错误；图片、字体和图标无 404。
- [ ] 不改变现有 API 请求数量、请求参数、权限过滤和数据持久化结果。
- [ ] 关键表单可用键盘完成，焦点状态可见，图标按钮有可访问名称。

## 12. 交付物

- `frontend_proposal.md`：本需求文档（当前文件）。
- 更新后的 `frontend/index.html`、`frontend/assets/styles.css`、`frontend/js/app.js` 及必要的视图文件。
- 院徽静态资源及引用路径说明。
- 至少一份跨视口截图或手工验收记录，覆盖登录页、工作台、AI 对话和一个管理/列表页面。
- 一份简短变更说明，列出新增依赖、已验证浏览器和已知限制。

